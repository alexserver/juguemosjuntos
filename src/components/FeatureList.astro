---
// Feature list item with icon for "Qué incluye" section
interface Props {
  icon: string;
  text: string;
  textClass?: string;
}

const { icon, text, textClass = '' } = Astro.props;
---

<div class="flex items-start gap-4">
  <div class="text-3xl flex-shrink-0">{icon}</div>
  <p class:list={["text-lg text-gray-700 pt-1", textClass]}>{text}</p>
</div>
