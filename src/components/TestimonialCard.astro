---
// Testimonial card for customer quotes
interface Props {
  quote: string;
  author: string;
}

const { quote, author } = Astro.props;
---

<div class="bg-white rounded-2xl p-6 shadow-lg">
  <div class="text-4xl text-brand-orange mb-4">"</div>
  <p class="text-gray-700 text-lg italic mb-4">{quote}</p>
  <p class="text-brand-purple font-bold">— {author}</p>
</div>
