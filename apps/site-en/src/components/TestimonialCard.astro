---
// Testimonial card for customer quotes
interface Props {
  quote: string;
  author: string;
  cardClass?: string;
  quoteMarkClass?: string;
  quoteClass?: string;
  authorClass?: string;
}

const {
  quote,
  author,
  cardClass = "bg-white",
  quoteMarkClass = "text-brand-orange",
  quoteClass = "text-gray-700",
  authorClass = "text-brand-purple",
} = Astro.props;
---

<div class:list={[cardClass, "rounded-2xl p-6 shadow-lg"]}>
  <div class:list={["text-4xl mb-4", quoteMarkClass]}>"</div>
  <p class:list={["text-lg italic mb-4", quoteClass]}>
    {quote}
  </p>
  <p class:list={["font-bold", authorClass]}>— {author}</p>
</div>
