---
// Feature list item with icon for "Qué incluye" section
interface Props {
  icon: string;
  text: string;
  textClass?: string;
}

const { icon, text, textClass = 'text-gray-700' } = Astro.props;
---

<div class="flex items-start gap-4">
  <div class="text-3xl shrink-0">{icon}</div>
  <p class:list={["text-lg pt-1", textClass]}>{text}</p>
</div>
